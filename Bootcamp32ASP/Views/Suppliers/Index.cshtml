@*@model IEnumerable<Bootcamp32ASP.Models.tb_m_supplier>*@

@{
    ViewBag.Title = "Index Suppliers";
}

<script src="//ajax.googleapis.com/ajax/libs/jquery/1.11.1/jquery.min.js"></script>
<link href="~/Content/dataTables.bootstrap.min.css" rel="stylesheet" />
<link href="~/Content/responsive.bootstrap.min.css" rel="stylesheet" />
<script src="~/Content/jquery.dataTables.min.js"></script>
<script src="~/Content/dataTables.bootstrap4.min.js"></script>

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create", null, new { @class = "btn btn-primary" })
    <button type="button" class="btn btn-primary" data-toggle="modal" data-target="#myModal">Create New</button>
</p>

<div class="container">
    <!-- Modal -->
    <div class="modal fade" id="myModal" role="dialog">
        <div class="modal-dialog">
            <!-- Modal content-->
            <div class="modal-content">
                <div class="modal-header">
                    <button type="button" class="close" data-dismiss="modal">&times;</button>
                    <h4 class="modal-title">Create New Supplier</h4>
                </div>
                <div class="modal-body">
                    <div class="container">
                        <form>
                            <div class="form-group">
                                <label for="Name">Supplier Name:</label>
                                <input type="text" class="form-control" id="Name" placeholder="Enter Supplier Name" name="Name">
                            </div>
                            <div class="form-group">
                                <label for="Email">Email:</label>
                                <input type="email" class="form-control" id="Email" placeholder="Enter Supplier Email" name="Email">
                            </div>
                        </form>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="submit" class="btn btn-success" data-dismiss="modal">Submit</button>
                </div>
            </div>

        </div>
    </div>

</div>

<table id="dataGrid" class="table table-striped table-bordered dt-responsive nowrap">
    <thead style="background-color:burlywood; color:white">
        <tr>
            <th>
                @*@Html.DisplayNameFor(model => model.Name)*@
                Supplier Name
            </th>
            <th>
                @*@Html.DisplayNameFor(model => model.Email)*@
                Email
            </th>
            <th>
                @*@Html.DisplayNameFor(model => model.CreateDate)*@
                Create Date
            </th>
            <th>
                Action
            </th>
        </tr>
    </thead>
    <tbody style="background-color:khaki" class="tbody">
        @*@foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Email)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.CreateDate)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.Id }, new { @class = "btn btn-warning" })
                    @Html.ActionLink("Details", "Details", new { id = item.Id }, new { @class = "btn btn-info" }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.Id }, new { @class = "btn btn-danger" })
                </td>
            </tr>
        }*@
    </tbody>

</table>

<div id="myModal" class="modal fade" role="dialog">
    <div class="modal-dialog">
        <!-- Modal content -->
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal">&times;</button>
                <h4 class="modal-title"></h4>
            </div>
        </div>
    </div>
</div>

<script type="text/javascript">
    $(document).ready(function () {
        loadData();
    });
    $('#dataGrid').dataTable({
        "columnDefs": [
            { "orderable": false, "targets": 4 }
        ]
    });
</script>

@section scripts{
    <script>
        //Show Data Table
        function loadData() {
            $.ajax({
                url: "/Suppliers/List",
                type: "GET",
                contentType: "application/json;charset=utf-8",
                dataType: "json",
                success: function (result) {
                    var html = '';
                    const obj = JSON.parse(result);
                    $.each(obj, function (key, supplier) {
                        html += '<tr>';
                        html += '<td>' + supplier.Name + '</td>';
                        html += '<td>' + supplier.Email + '</td>';
                        html += '<td>' + suuplier.CreateDate + '</td>';
                        html += '<td>test</td>';

                        //html += '<td> <a href="#" class="btn btn-success" onclick="return getbyID(' + supplier.Id + ')"><i class="fa fa-pencil">Edit</i></a> | <a href="#" class="btn btn-danger" onclick="Delete(' + supplier.Id + ')"><i class="fa-trash-o">Delete</i></a></td >';
                        html += '</tr>';
                    });
                    $('.tbody').html(html);
                },
                error: function (errormessage) {
                    console.log(errormessage.responseText);
                }
            });
        }

        //Show Modal
        function addNewItem() {
            $("#newItemModel").modal();
        }

        function saveOrder(data) {
            return $.ajax({
                contentType: 'application/json; charset=utf-8',
                dataType: 'json',
                type: 'POST',
                url: "/Items/SaveOrder",
                data: data,
                success: function (result) {
                    alert(result);
                    location.reload();
                },
                error: function () {
                    alert("Error!")
                }
            });
        }

        $("#saveItem").click(function (e) {
            e.preventDefault();

            var itemArr = [];
            itemArr.length = 0;

            $.each($("#detailsTable tbody tr"), function () {
                itemArr.push({
                    NameItem: $(this).find('td:eq(0)').html(),
                    Price: $(this).find('td:eq(1)').html(),
                    Stock: $(this).find('td:eq(2)').html(),
                    Supplier_Id: $(this).find('td:eq(3)').html(),
                });
            });

            $.when(saveItem(data)).then(function (response) {
                console.log(response);
            }).fall(function (err) {
                console.log(err);
            })
        });
    </script>
}

